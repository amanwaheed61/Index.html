name: Deploy HTML File

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to GitHub Pages
        run: |
          cp -R * public/ # Assuming 'public/' is your target directory for deployment
